// why do we need asynchronous since java script is single threaded whenever we perform heavy IO
// functions the other code execution will be stopped by the js
// What to do now
// Here comes the asynchronous function
// Promises -> Those are the foundation of the asynchronous programming
// Promise is an object which will be returned by an async function which will
// Generally has the state of the function
// Promise has 3 states -> Pending , Fulfilled (Resolved) and Rejected