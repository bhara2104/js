let a = "a" ?? "bharat"

console.log(a)

const b = null ?? "Bhara";
console.log(b)

// Null coalescing operators are used to check null objects it is something like safe navigation which
// Is used to check whether a value is null or not

// There is another function which is optional chaining .? which is used to check whether an object is null or not
